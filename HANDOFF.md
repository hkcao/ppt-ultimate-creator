# 维护交接

## 当前状态 · 2026-09-13

- 技能源码：`skills/ppt-ultimate-creator/`，共 16 个文件（不计系统隐藏文件）；中文名为终极ppt生成。
- 本机安装位置：`~/.codex/skills/ppt-ultimate-creator/`；运行时模板库：`~/.ppt-ultimate-creator/templates/{defaults,custom}/`。
- 用户已否决首批下载模板。5 份原件、默认库条目、研究副本及预览已删除，仅保留模板根目录；用户自定义内容未改动。仓库仅保留空模板目录，新增模板文件默认不提交。
- 用户授权创建新的私有 GitHub 仓库并提交；已创建 `https://github.com/hkcao/ppt-ultimate-creator`，GitHub API 核实 `isPrivate=true`、`visibility=PRIVATE`。实现提交 `709af63` 已推送至 `main`；本次文档提交记录该发布状态。远端为 origin，后续以 `git status` 与远端分支 SHA 核实最新同步情况。未授权公开发布。

## 已确认设计

- 六阶段：来源 → 需求/逐页大纲 → HTML → 真实 AI 高保真图 → 原生可编辑 PPT → 实际渲染与对照修复。
- 内容准确 > 语义编辑性 > 视觉还原。保存独立内容规格，不让图片识别结果重新定义事实；允许并说明小视觉差异。
- 原文实验图表默认沿用原件；需要重建且有核实数据的数据折线用原生图表，折线路径尽量单个连接符/自由曲线，避免多线段拼接。图片可替换不等于内部可编辑。
- 用户可提供逐页要求，也可由技能起草。确认与版本保存在每个制作项目内；默认分阶段确认，用户明确调整时遵循，不添加工具批准关卡。
- 模板与技能代码独立，空库不阻塞；未来模板由用户放入或明确要求导入。
- 技能为工作流与参考文档，不包含自定义 PPT 引擎，不依赖旧图片转换技能。

## 本轮优化

- 增加 `references/design-methods.md`：MIT 与 Duarte 的来源链接、主线和视觉层级、现场汇报/自主阅读的差异、五类用途的表达建议。
- HTML 增加缩略总览和共享字体/配色/边距规范；复杂原生对象在样张阶段做局部可编辑实现验证；仅重做受影响页面。
- 方法研究不等于审美保证，最终效果仍以样张反馈和真实 PPT 渲染为准。未重新预装任何风格模板。

## 验证与边界

- 第一版官方结构校验通过。独立模拟任务在阶段 2 正确等待大纲确认，4 页缓存实验内容与数值保留，未伪造确认或提前生成 HTML/图片/PPT。
- 本轮官方校验返回 `Skill is valid!`；首版 6 个技能文件与当时安装副本逐字节一致；Markdown 相对链接有效；运行时 defaults/custom 均为空。
- 尚未运行真实 AI 生图 → PPTX → 实际渲染的完整端到端测试，不能宣称视觉还原质量已经证明。
- 没有 CI、发布版本或 Release 资产。

## 后续维护

先核实 Git 与安装状态，修改仓库源码并校验，再同步安装副本。使用带 PyYAML 的 Python 运行 skill-creator 的 `scripts/quick_validate.py skills/ppt-ultimate-creator`；校验器来自当前 Codex 安装，不随仓库复制。

下一次用真实材料走完样张、重建和渲染流程，根据实际缺陷窄修。此文档不授权后续公开发布或其他外部操作。

## 2026-09-13 实测反馈修正

- 用户报告未先交互、原文数据复用不足、生成慢；本轮修改为先理解关键原理并实际澄清范围/深度，再写逐页大纲，必要回答前不创建 slides.yaml。
- 原文实验图表默认截图/提取，保存源定位与哈希；需要图内编辑时核对数据和取舍。默认中文微软雅黑、英文 Times New Roman，检查实际字体可用性。
- 新增 execution.md、extract_pdf.py、storyboard.py；基础 HTML 仅用于内容检查，不冒充布局完成。多页 worker 独立输出，主 agent 持有共享规格并最终组装。
- 3 个脚本测试通过：PDF 裁剪/内容与来源哈希、越界页拒绝、HTML 文本保留/转义及防覆盖；官方校验通过。测试用 PyMuPDF 1.28.2 安装于临时隔离目录，非全局环境。
- 独立行为模拟在缺论文/受众信息时实际提问，未写大纲或产物。发现并修正并行授权措辞冲突；该模拟不是实际完整 PPT 测试。
- 本轮 9 个技能文件已同步安装并逐字节核对；本记录随本轮实现提交。上一已推送提交为 075227b，当前同步状态以 git status 和远端 SHA 为准。无速度或 token 基准数据，不声称具体收益。

## 2026-09-13 上下文、后端与确认优化

- 默认确认改为大纲+HTML 合并、AI 样张；仅需求有缺口时额外澄清。样张后内部检查全套并自动重建，生成参考与用户批准分开记录，详细模式仍可选。
- worker 强制无历史 fork，只接收页任务包、局部证据、样式、衔接与输出约束；按变化定点读取，简单页合批，纯网络任务不启动 agent。没有 token/速度实测收益数据。
- 新增 image-backend.md 与 generate_image.py，独立 JSON 配置 + 环境变量密钥，支持同步兼容 Images 文生图接口；不支持原生 Gemini、异步任务或图片编辑。无真实凭据，无付费调用；7 个测试通过，其中 4 个后端模拟测试。
- 官方校验通过。独立检查发现并清理 3 处额外确认措辞。后端只有模拟验证，实际提供商及完整 PPT 仍待验证。
- 本轮源码与交接一起提交，同步安装时应核对 11 个技能文件。上一提交 ebd37cd；最终提交与推送状态用 git status 和远端 SHA 核实。

## 公开仓库与 README 更新

- 用户明确授权将仓库改为公开；这是新的可见性决策，覆盖此前仅私有的限制。
- README 增加中英安装口令、调用样例与 AI 视觉概念展示。示例不是实际 PPTX 渲染或默认模板，不含下载模板原件。
- 初始澄清合并询问参考模板；用户没有模板或委托推荐后再给建议，不增加独立确认轮次。
- 本记录随源码更新提交；GitHub API 已核实 visibility=PUBLIC、isPrivate=false；实现提交 96e4149 已推送，本记录随文档提交同步。此前已推送 f3c2369。

## 分类建议独立维护

- references/styles/ 拆分 academic、product、solution、course、technical-planning 五份文档，分别维护样式、内容布局与检查重点。
- design-methods.md 保留通用原则和类别索引；技能按类别读取，混合场景按章节选用，不加载所有建议或增加确认轮次。
- README 双语索引同步更新。本轮为文档组织修改，校验相对链接与技能结构，不重新运行无关脚本测试；源码与安装副本同步后提交。上一已推送提交 f8f01bf。

## 平台无关多模态

- 当前模型/宿主的视觉输入、原生图像输出和编辑能力优先，不强制 GPT、OpenAI API、Codex 工具或兼容 API 脚本。
- 识别/生成/编辑分别判断；脚本暂不支持某协议不代表宿主原生能力不可用。实际图片结果与原文证据约束保留。
- 本轮为指令与双语说明窄修，执行结构/相对链接校验与安装一致性核对；随本轮提交同步，上一已推送 bc530c0。

## Kimi 实测反馈修正

- 用户仅要求修正技能，测试 PPT 用于诊断，不修改/公开测试材料。未获得 Kimi 对话与加载版本，不能把无交互原因确定为缺少 ask。
- 增加所有 Agent 通用的实际等待：普通文本提问后结束本轮，waiting_for_user；默认一次合并启动确认、一次大纲+HTML、一次样张，除非明确免交互。
- 截图非绝对规则：图例/结构不直白时允许重标注或数据核实的重绘，保留数值、单位、刻度与条件；缺少精确数据不得猜曲线。
- 公式独立数学字体/原生公式及实际渲染检查；测试页是普通文本 Unicode 公式，不能单凭 XML 或本机缺字确定 Kimi 根因。
- 学术指南集中维护通用大纲与逐步例子技巧，不固化临时用例或复制参考材料笔误。
- 本轮为文档规则修正，校验链接与技能结构；独立无 ask 行为模拟通过：普通问题后等待、不创建大纲/HTML/PPT；检查发现并清理两处可能误读为跳过启动确认的措辞。修改与本记录一并提交，上一已推送 7a06c9b。实际 Kimi 复测待用户新运行验证。

## 学术表达技巧归属

- 大纲组织和例子讲解集中在 styles/academic.md；从课程指南移除重复段落，README 仅链接对应指南。
- 方法泛化为目标、输入、对象、操作、状态变化和结果，不绑定临时测试材料、专有案例或特定领域。
- 本次修改与交接一并提交；上一已推送 a1b62a1。验证结构、链接和安装一致性，不改测试材料或 Git 历史。
